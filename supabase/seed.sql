-- Create users from Authentication > Users, then grant a trial during testing:
-- insert into public.subscriptions (user_id, plan_id, status, ends_at)
-- values ('USER_UUID', 'trial', 'trial', now() + interval '7 days');

